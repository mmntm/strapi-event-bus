const { BaseEvent } = require("./lib/models/Event.model")

/**
 * Type of virutal node used to define elements to create.
 * @typedef { import('./lib/models/Event.typings').BaseEventConstructorInput } BaseEventConstructorInput
 */

/**
 * Type of virutal node used to define elements to create.
 * @typedef { import('./lib/models/Event.typings').EventType } EventType
 */

/**
 * Type of virutal node used to define elements to create.
 * @typedef { import('./lib/models/Event.typings').BasicEventConstructorInput } BasicEventConstructorInput
 */

module.exports = {
	BaseEvent
}